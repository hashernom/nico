# Dependencias
node_modules/

# Build
dist/

# Variables de entorno (¡nunca subir!)
.env
.env.local
.env.*.local

# Vercel
.vercel

# Sistema
.DS_Store
Thumbs.db
*.log

# Local Netlify folder
.netlify

# Caché local del Supabase CLI (proyecto vinculado, versiones, pooler URL).
# Se regenera solo con `supabase link` / `supabase db push`; no es código.
supabase/.temp/
